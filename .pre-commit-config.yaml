repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
        exclude: ^legacy/
      - id: trailing-whitespace
        exclude: ^legacy/
      - id: check-yaml
      - id: check-added-large-files
        args: ["--maxkb=5000"]
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: uv run ruff format
        language: system
        types: [python]
      - id: ruff-check
        name: ruff check
        entry: uv run ruff check --fix
        language: system
        types: [python]
      - id: leakage-lint
        name: leakage lint
        entry: uv run python .claude/skills/leakage-guard/scripts/leakage_lint.py
        language: system
        files: ^src/nvquant/.*\.py$
